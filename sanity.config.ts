// This file was used for an embedded Sanity Studio route.
// The website build on Vercel does not include Sanity Studio dependencies.
// Use the standalone Studio project in /studio (run `npm install` and `npm run dev` inside that folder).

export {}
